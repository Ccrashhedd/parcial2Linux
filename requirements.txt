# Dependencias del proyecto - Sistema Operativo Parcial 2

# Base de datos (comentado porque requiere pg_config del sistema)
# Para habilitarlo: instala libpq-dev (Ubuntu) o postgresql-devel (Fedora)
# psycopg2-binary==2.9.9

# Interfaz gráfica (TKinter viene incluido en Python)
# No necesita instalación adicional en la mayoría de casos

# Utilidades opcionales
python-dotenv==1.0.0

# Herramienta para crear ejecutables
pyinstaller
